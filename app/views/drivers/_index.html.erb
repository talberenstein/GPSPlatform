<div class="current-menu-config" id="config-menu-drivers" hidden>
  <blockquote>Conductores</blockquote>
  <table class="bordered" id="drivers_table">
    <thead>
    <tr>
      <th class="">Nombre</th>
      <th class="">RUT</th>
      <th class="">Compañía</th>
      <th class=""></th>
      <th class=""></th>
      <th>
        <button class="tooltipped" data-tooltip="Agregar Conductor" data-target="new_driver_modal">
          <%= image_tag 'new.svg', width: 30 %>
        </button>
      </th>
    </tr>
    </thead>
    <tbody>
    <% @drivers.each do |driver| %>
        <%= render 'drivers/driver_row_config', driver: driver %>
    <% end %>
    </tbody>
  </table>

</div>

<%= render 'drivers/form_new' %>
<%= render 'drivers/form_edit' %>

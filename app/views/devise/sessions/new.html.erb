<div class="row">
  <div class="col s6 offset-s3">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class='row'>
      <div class="col s12">
        <input class='' type='email' name='user[email]' id='email' placeholder="Correo electrónico" required/>  
      </div>
      
    </div>

    <div class='row'>
      <div class="col s12">
      <input class='' type='password' name='user[password]' id='password' placeholder="Password" required/>
      </div>
    </div>

    <% if not devise_mapping.rememberable? -%>
      <div class="row">          
        <div class="input-field col s12">
            <input type="checkbox" id="remember-me" name="user[remember_me]" />
            <label for="remember-me">Recordarme</label>
        </div>
      </div>
    <% end %>

    <div class='row'>
      <div class="col s12">
        <button type='submit' name='btn_login' class='col s12 btn btn-large waves-effect indigo'>Ingresar</button>
      </div>
    </div>
  <% end %>
  </div>
</div>